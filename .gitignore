# Datos locales (exportaciones con RUT y datos de clientes — no subir al repo)
/data/

# Bandeja de descargas a archivar (mismos datos sensibles que /data)
/inbox/

# Reportes generados (traen RUT y datos de clientes — no subir al repo público)
/reportes/

# Material interno de comisiones (montos por vendedor — no subir al repo público)
docs/Comisiones_v1_*.pptx
docs/Comisiones_v1_*.docx

# Logs y artefactos del ETL (pueden traer nombres de vendedores)
etl_run.log
etl_historico.log
etl_no_mapeados.csv
etl_historico_no_mapeados.csv

# Variables de entorno con secretos
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Sistema operativo
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml
